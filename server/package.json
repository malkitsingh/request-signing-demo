{
  "name": "server",
  "version": "1.0.0",
  "description": "a demo exoress server that will expose a route and tries to match the request HASH",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "HMAC",
    "Request",
    "Signing",
    "API"
  ],
  "author": "Malkit",
  "license": "ISC",
  "dependencies": {
    "express": "^4.17.2"
  }
}
